{
  "name": "proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless API proxy — Firebase Admin + AI placeholder",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "@vercel/node": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0"
  }
}
